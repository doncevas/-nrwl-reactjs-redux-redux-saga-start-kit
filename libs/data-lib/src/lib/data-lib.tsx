import React from 'react';

import './data-lib.scss';

/* eslint-disable-next-line */
export interface DataLibProps {}

export const DataLib = (props: DataLibProps) => {
  return (
    <div>
      <h1>Welcome to data-lib component!</h1>
    </div>
  );
};

export default DataLib;
